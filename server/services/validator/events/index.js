const deleteEventValidator = require("./delete-event-validator");
const getEventValidator = require("./delete-event-validator");

const createEventValidator = require("./new-event-validator");
const reviewValidator = require("./review-event-validator");

module.exports = {
  deleteEventValidator,
  getEventValidator,
  createEventValidator,
  reviewValidator,
};
